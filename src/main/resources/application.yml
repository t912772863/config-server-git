eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:1001/eureka/

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/t912772863/spring-cloud-config.git
          search-paths:
      label: master

server:
  port: 1201